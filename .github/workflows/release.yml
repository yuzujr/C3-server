name: release

on:
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  build:
    name: build-${{ matrix.goos }}-${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            ext: ''
            archive: tar.gz
          - goos: linux
            goarch: arm64
            ext: ''
            archive: tar.gz
          - goos: darwin
            goarch: amd64
            ext: ''
            archive: tar.gz
          - goos: darwin
            goarch: arm64
            ext: ''
            archive: tar.gz
          - goos: windows
            goarch: amd64
            ext: .exe
            archive: zip

    env:
      BINARY_NAME: c3-server

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          mkdir -p build dist
          go build -trimpath -ldflags "-s -w" -o "build/${BINARY_NAME}${{ matrix.ext }}" ./cmd/server

      - name: Package
        env:
          VERSION: ${{ github.ref_name }}
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          PKG_DIR="${BINARY_NAME}_${VERSION}_${GOOS}_${GOARCH}"
          mkdir -p "package/${PKG_DIR}"
          cp "build/${BINARY_NAME}${{ matrix.ext }}" "package/${PKG_DIR}/"
          cp -r web docs "package/${PKG_DIR}/"
          cp .env.example README.md LICENSE "package/${PKG_DIR}/"

          if [ "${{ matrix.archive }}" = "zip" ]; then
            (cd package && zip -r "../dist/${PKG_DIR}.zip" "${PKG_DIR}")
          else
            (cd package && tar -czf "../dist/${PKG_DIR}.tar.gz" "${PKG_DIR}")
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            dist/*.tar.gz
            dist/*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: dist-*
          merge-multiple: true
          path: dist

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.tar.gz
            dist/*.zip
          generate_release_notes: true
